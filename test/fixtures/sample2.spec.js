describe('dummy test', () => {
    beforeAll(() => {
    })

    beforeEach(() => {
    })

    it('sample test', () => {
        expect(browser.command(1)).toBe(2)
    })

    afterEach(() => {
    })

    afterAll(() => {
    })
})
